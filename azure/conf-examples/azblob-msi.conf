# mount 인스턴스 설정: Managed Identity 인증 (Azure VM 전용)
# 파일 위치: /etc/rclone/mounts/azblob-msi.conf
# 서비스: systemctl start rclone-mount@azblob-msi.service

REMOTE=azblob-msi-system:mycontainer
MOUNTPOINT=/mnt/azureblob-msi
EXTRA_ARGS=--allow-other --vfs-cache-mode writes
